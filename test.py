# teste 1 python reprodução
npp_seed = 0
npls = 3
npp_pls = [10, 80, 5]
mass_seed = [0.1, 1, 3] #atributo variante 
#npp = sum(npp_pls)
for npp_rep in npp_pls:
    npp_rep = npp_rep *0.1






#como fazer calcular 2 listas (arrays), primeiro c primeiro ...

 #   nseeds = []
#for npp_rep, mass_seed in zip(npp_rep, mass_seed):
 #   nseeds.append(npp_rep + mass_seed) 

 # npp_rep[:] = [float(i) / mass_seed for i in npp_rep]
 
#nseeds = [npp_rep % mass_seed for npp_rep, mass_seed in zip(npp_rep, mass_seed)]
#for number in range(mass_seed)
#nseeds = map(sum, zip(npp_rep, mass_seed))

 #   nseeds = npp_rep/mass_seed
 #  print(npp_rep)
 # print(nseeds)


#def rep(npp):
#    if npp > 0: 
#        npp_seed = npp*0.1
#        print('npp total', npp)
 #       print('10% npp é', npp_seed)
  #  else: 
   #     print('não ha sementes')

rep(npp)


